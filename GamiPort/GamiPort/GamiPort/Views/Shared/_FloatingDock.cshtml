@* 右下浮動工具列 + 多個聊天小視窗（FB 風格） *@
<div id="chat-row" class="chat-row" aria-live="polite"></div>

<div id="fab-dock" class="fab-dock" data-autohide-ms="10000">
    <!-- 主 FAB -->
    <button id="fabMain" type="button" class="btn btn-primary rounded-circle shadow fab-main"
            title="工具" aria-expanded="false" aria-controls="fabList">
        <i class="bi bi-chat-dots"></i>
    </button>

    <!-- 展開的小圓鈕 -->
    <div id="fabList" class="fab-list" aria-hidden="true">
        <button class="btn btn-light rounded-circle shadow fab" data-action="open-chat" data-title="對話 A" title="對話 A">
            <i class="bi bi-person"></i>
        </button>
        <button class="btn btn-light rounded-circle shadow fab" data-action="open-chat" data-title="對話 B" title="對話 B">
            <i class="bi bi-people"></i>
        </button>
        <button class="btn btn-light rounded-circle shadow fab" data-action="open-chat" data-title="對話 C" title="對話 C">
            <i class="bi bi-person-hearts"></i>
        </button>
        <button class="btn btn-light rounded-circle shadow fab" data-action="open-chat" data-title="對話 D" title="對話 D">
            <i class="bi bi-robot"></i>
        </button>
        <button class="btn btn-light rounded-circle shadow fab" data-action="open-chat" data-title="工作面板" title="工作面板">
            <i class="bi bi-kanban"></i>
        </button>
    </div>
</div>

<!-- 小視窗模板 -->
<template id="chat-template">
    <div class="chat-win shadow">
        <div class="chat-head">
            <div class="d-flex align-items-center gap-2">
                <!-- 召喚浮動按鈕 -->
                <button type="button" class="btn btn-light btn-sm rounded-circle chat-dock-toggle" title="工具">
                    <i class="bi bi-grid-3x3-gap"></i>
                </button>
                <strong class="chat-title">對話</strong>
            </div>
            <div class="ms-auto btn-group btn-group-sm">
                <button type="button" class="btn btn-light chat-min" title="最小化"><i class="bi bi-dash"></i></button>
                <button type="button" class="btn btn-light chat-close" title="關閉"><i class="bi bi-x-lg"></i></button>
            </div>
        </div>

        <div class="chat-body">
            <div class="chat-scroll small text-muted p-3">
                這裡是示意內容。你之後可以把真實訊息/工具 UI 放進來。
            </div>
            <div class="chat-input border-top p-2 d-flex gap-2">
                <input class="form-control form-control-sm" placeholder="輸入訊息..." />
                <button class="btn btn-primary btn-sm">送出</button>
            </div>
        </div>
    </div>
</template>
